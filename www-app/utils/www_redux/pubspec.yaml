name: www_redux
description: Base Redux abstractions
publish_to: none

environment:
  sdk: ^3.4.1
  flutter: ^3.22.1

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  flutter_redux: ^0.10.0

dev_dependencies:
  www_analysis:
    path: ../www_analysis
